import type { GNode } from './schema';

const P = (
  id: string,
  name: string,
  domainId: string,
  parentId: string,
  shortExplanation: string,
  whyItMatters: string,
  useCases: string[],
  tradeoffs: string[],
  failureModes: string[],
  tags: string[] = [],
): GNode => ({
  id,
  name,
  kind: 'pattern',
  domainId,
  parentId,
  level: 2,
  difficulty: 'advanced',
  layer: 'architectural',
  tags: ['pattern', ...tags],
  shortExplanation,
  whyItMatters,
  useCases,
  tradeoffs,
  failureModes,
  interviewRelevance: 5,
  productionRelevance: 5,
});

export const patterns: GNode[] = [
  P('pat-layered', 'Layered architecture', 'system-design', 'sd-architecture', 'Stack of presentation / domain / data layers with downward dependencies.', 'Defaults are fine for most CRUD apps; trains teams in separation of concerns.', ['Most line-of-business web apps', 'Service templates'], ['Layers can become anemic', 'Cross-cutting concerns leak'], ['Tight DB coupling in every layer']),
  P('pat-hexagonal', 'Hexagonal / Ports & Adapters', 'system-design', 'sd-architecture', 'Domain core isolated behind ports; adapters plug in DBs, queues, HTTP.', 'Lets you swap infra without touching domain logic; tests run without I/O.', ['Domains with rich rules', 'Long-lived services likely to outlive their infra'], ['More boilerplate up front', 'Wrong port boundaries lock you in differently'], ['Over-abstraction obscures intent']),
  P('pat-clean', 'Clean architecture', 'system-design', 'sd-architecture', 'Concentric layers with dependency rule pointing inward; use cases drive design.', 'Disciplined version of hexagonal; good for big org-wide service templates.', ['Enterprise services with many use cases', 'Heterogeneous infra'], ['Ceremony heavy for simple CRUD', 'Easy to misapply'], ['Use-case classes that just wrap repositories']),
  P('pat-microservices', 'Microservices', 'system-design', 'sd-architecture', 'Independently deployable services aligned with bounded contexts.', 'Org-scale tool: lets independent teams ship independently — when boundaries are right.', ['Many teams', 'Independent scaling needs', 'Polyglot tech needs'], ['Distributed system tax', 'Schema/version sprawl', 'Operational complexity'], ['Distributed monolith if coupling is wrong']),
  P('pat-modulith', 'Modular monolith', 'system-design', 'sd-architecture', 'Single deployable with strict internal module boundaries.', 'Captures most microservice benefits at a fraction of the operational cost.', ['Small-medium teams', 'Early-stage products', 'High-coupling domains'], ['Single deploy still couples lifecycles', 'Cultural discipline required'], ['Modules quietly become spaghetti']),
  P('pat-event-sourcing', 'Event sourcing', 'backend-eng', 'be-patterns', 'Store changes as an append-only event log; derive state via replay.', 'Perfect audit, time-travel debugging, multiple read models.', ['Financial systems', 'Audit-critical workflows', 'Multi-view analytics'], ['Schema evolution is hard', 'Read models need projection care', 'Snapshots required'], ['Compatibility-breaking event versions']),
  P('pat-cqrs', 'CQRS', 'backend-eng', 'be-patterns', 'Separate write model from read model(s).', 'Lets you optimize reads and writes independently; pairs naturally with event sourcing.', ['Heavily skewed read:write workloads', 'Many read shapes from one write source'], ['Eventually consistent reads', 'More moving parts'], ['Stale reads surprising users']),
  P('pat-saga', 'Saga pattern', 'distributed-systems', 'ds-coordination', 'Long-running, distributed transactions composed of local txns with compensations.', 'Replaces 2PC with eventually-consistent business workflows.', ['Order processing', 'Travel booking', 'Multi-service workflows'], ['Compensation design is hard', 'Partial failures observable to users'], ['Lost or duplicate compensation steps']),
  P('pat-circuit-breaker', 'Circuit breaker', 'distributed-systems', 'ds-resilience', 'Open the circuit when a dependency keeps failing; fail fast instead of piling up.', 'Stops cascading failure from a sick downstream.', ['Any call to a remote dependency', 'Critical-path service-to-service calls'], ['Tuning thresholds is non-trivial', 'Need fallbacks for open state'], ['Stuck-open breaker; stuck-closed breaker (no signal)']),
  P('pat-bulkhead', 'Bulkhead', 'distributed-systems', 'ds-resilience', 'Isolate resources (thread pools, queues) per dependency or tenant.', 'A failing partition doesn\'t take down the rest.', ['Multi-tenant systems', 'Critical vs non-critical traffic separation'], ['Higher resource floor', 'Capacity planning per bulkhead'], ['Bulkheads sized too small block legitimate traffic']),
  P('pat-retry-backoff', 'Retry with backoff + jitter', 'distributed-systems', 'ds-resilience', 'Re-send failed requests with exponentially-growing waits + randomization.', 'Recover transient failures without becoming a self-DDoS.', ['Any unreliable downstream', 'Network-spanning calls'], ['Increases tail latency', 'Hard to tune without metrics'], ['Naive infinite retry causes retry storms']),
  P('pat-rate-limit', 'Rate limiting', 'distributed-systems', 'ds-resilience', 'Cap requests per key/tenant/route to protect downstream and ensure fairness.', 'Defends against abusive clients and saves the system from itself.', ['Public APIs', 'Per-tenant quotas', 'Internal protection'], ['Wrong limits anger legitimate users', 'Algorithm choice matters (token bucket vs leaky bucket)'], ['Limiter itself becomes bottleneck']),
  P('pat-load-shed', 'Load shedding', 'distributed-systems', 'ds-resilience', 'Reject low-priority requests when overloaded to preserve high-priority ones.', 'You can\'t fit infinity into finite capacity; shed predictably, not by collapse.', ['High-traffic events', 'When upstream is healthy but you are overloaded'], ['Shedding criteria must reflect business priority', 'Visible to users'], ['Shedding the wrong traffic; cliff behavior']),
  P('pat-api-gateway', 'API gateway', 'api-design', 'api-arch', 'Single ingress that handles auth, routing, rate limiting, schema translation.', 'Standardizes cross-cutting concerns across many services.', ['Multi-service products', 'External API surfaces'], ['Becomes a bottleneck', 'Risk of dumping logic into the gateway'], ['Gateway-as-a-monolith anti-pattern']),
  P('pat-service-mesh', 'Service mesh', 'distributed-systems', 'ds-platform', 'Sidecar proxies handle mTLS, retries, routing, telemetry between services.', 'Moves resilience and security out of app code into the platform.', ['Polyglot fleets', 'Heavy east-west traffic'], ['Operational complexity', 'Sidecar resource overhead'], ['Mesh control plane outage halts deploys']),
  P('pat-sidecar', 'Sidecar pattern', 'kubernetes', 'k8s-patterns', 'A helper container runs alongside the app and provides cross-cutting features.', 'Decouples ops concerns (logging, proxying) from app code.', ['Log shipping', 'Service mesh proxy', 'Auth/identity injection'], ['Lifecycle coupling pitfalls', 'Resource accounting per pod'], ['Sidecar crash loops dragging the pod down']),
  P('pat-lambda-arch', 'Lambda architecture', 'data-eng', 'de-patterns', 'Parallel batch + speed layers serve a unified query layer.', 'Historically the way to combine accuracy and freshness.', ['Pre-streaming-era systems', 'When batch correctness was non-negotiable'], ['Two pipelines to maintain', 'Reconciliation drift'], ['Speed and batch disagree on the same record']),
  P('pat-kappa-arch', 'Kappa architecture', 'streaming', 'stream-patterns', 'A single streaming pipeline replayed for both fresh and historical views.', 'Simpler than Lambda when your streaming engine can replay.', ['Modern Flink/Spark-streaming stacks'], ['All-or-nothing on streaming maturity', 'Operational discipline on replay'], ['Reprocessing storms']),
  P('pat-medallion', 'Medallion architecture (bronze/silver/gold)', 'lakehouse', 'lake-patterns', 'Raw → cleaned → curated layered tables in a lakehouse.', 'Provides consistent quality boundaries for analytics and ML features.', ['Lakehouse implementations', 'Multi-tenant data platforms'], ['Layer proliferation if not disciplined', 'Latency through layers'], ['Bronze becoming the production table']),
  P('pat-lakehouse', 'Lakehouse architecture', 'lakehouse', 'lake-patterns', 'Open table format on object storage with ACID + schema + time travel.', 'Unifies data lake economics with warehouse semantics.', ['Cost-sensitive analytics + ML on the same data'], ['Operational complexity vs pure warehouse', 'Small-file problem'], ['Compaction debt; metadata bloat']),
  P('pat-feature-store-arch', 'Feature store architecture', 'mlops', 'mlops-features', 'Unified registry + offline (training) + online (serving) store for features.', 'Eliminates train/serve skew and enables feature reuse across teams.', ['Multi-team ML platforms', 'Online inference'], ['Operational complexity', 'Cost of low-latency online store'], ['Schema drift between offline and online definitions']),
  P('pat-model-serving', 'Model serving architecture', 'mlops', 'mlops-serving', 'Versioned, observable, scalable HTTP/gRPC serving for ML models with canary/shadow.', 'Treats models like first-class services with SLOs and rollback.', ['Online inference', 'A/B and shadow tests'], ['GPU vs CPU vs batch tradeoffs', 'Cold-start with large models'], ['Bad model version pinned in production']),
  P('pat-rag', 'RAG architecture', 'genai', 'genai-rag', 'Retrieve relevant context → augment prompt → generate grounded answer.', 'Grounds LLMs in your data without fine-tuning.', ['QA over docs', 'Customer support copilots', 'Search-augmented chat'], ['Retrieval quality is the bottleneck', 'Prompt engineering ceremony'], ['Hallucinations from irrelevant retrieval; prompt injection']),
  P('pat-agentic', 'Agentic workflow', 'genai', 'genai-agents', 'LLM decides which tools to call in a loop until task is complete.', 'Lets language understanding orchestrate side-effecting tools.', ['Multi-step research tasks', 'Customer ops automation'], ['Non-determinism', 'Cost and latency variance', 'Hard to evaluate'], ['Tool misuse from prompt injection; infinite loops; cost runaway']),
  P('pat-hitl', 'Human-in-the-loop', 'prod-ai', 'pai-hitl', 'Route low-confidence or high-risk outputs to humans for review.', 'Bridges model quality and product trust; collects labels for next train.', ['Content moderation', 'Medical / legal copilots', 'Loan decisions'], ['Cost of review', 'Latency to user', 'Reviewer drift'], ['Reviewers rubber-stamping; bias amplification']),
];

export const patternNodes = patterns;
