import type { GNode } from './schema';

const F = (
  id: string,
  name: string,
  domainId: string,
  parentId: string,
  shortExplanation: string,
  whyItMatters: string,
  failureModes: string[],
  prevention: string[],
  metricsToMonitor: string[],
  tags: string[] = [],
): GNode => ({
  id,
  name,
  kind: 'failureMode',
  domainId,
  parentId,
  level: 2,
  difficulty: 'advanced',
  layer: 'operational',
  tags: ['failure-mode', ...tags],
  shortExplanation,
  whyItMatters,
  failureModes,
  useCases: prevention,
  metricsToMonitor,
  interviewRelevance: 5,
  productionRelevance: 5,
});

export const failureModes: GNode[] = [
  F('fm-db-bottleneck', 'Database bottleneck', 'databases', 'db-scaling', 'A single DB instance becomes the throughput or latency ceiling for the system.', 'In most production incidents, the actual bottleneck is the DB, not the app.', ['Connection pool exhaustion', 'Long-running transactions block writers', 'Index missing on hot query', 'Lock contention on shared rows'], ['Add read replicas; route reads with care', 'Cache the read path', 'Partition / shard hot tables', 'Use connection poolers like PgBouncer'], ['Active connections', 'Tx time p99', 'Buffer cache hit ratio', 'Lock waits'], ['db', 'capacity']),
  F('fm-cache-stampede', 'Cache stampede', 'caching', 'cache-failures', 'Many clients miss the cache simultaneously and overload the origin.', 'Common on cache expiry of a hot key — origin DB can melt in seconds.', ['All clients recompute the same value at once', 'Downstream DB CPU spikes', 'Tail latency explodes during stampede window'], ['Probabilistic early expiration', 'Request coalescing / singleflight', 'Stale-while-revalidate', 'Locking on regeneration'], ['Cache miss rate spikes', 'Origin QPS spikes', 'Per-key request count'], ['cache']),
  F('fm-hot-partition', 'Hot partition / hot key', 'databases', 'db-sharding', 'A disproportionate share of traffic lands on one shard or partition.', 'Defeats horizontal scaling — you bought 100 nodes but one is overloaded.', ['Celebrity user / influencer effect', 'Time-bucket keys at "now"', 'Sequential IDs in DynamoDB-style stores'], ['Hash-prefix the partition key', 'Add per-key cache', 'Pre-split partitions for known hotspots'], ['Per-partition QPS', 'Per-partition latency', 'Throttle counts'], ['db', 'scale']),
  F('fm-queue-backlog', 'Queue backlog', 'messaging', 'msg-monitoring', 'Producers outpace consumers; queue depth grows unboundedly.', 'Becomes an outage when memory/disk fills or downstream SLOs miss freshness.', ['Slow downstream service', 'Sudden traffic spike beyond consumer capacity', 'Poison message blocks partition'], ['Auto-scale consumers on lag', 'Dead-letter queues for poison messages', 'Backpressure to producers', 'Load shedding upstream'], ['Consumer lag', 'Queue depth', 'Message age p99'], ['messaging']),
  F('fm-data-skew', 'Data skew', 'data-eng', 'de-quality', 'Some partitions or keys carry vastly more data than others.', 'Causes job runtime to be set by the slowest task — wall time explodes.', ['One country / tenant has 1000x rows', 'Default value sucks all NULLs into one bucket', 'Skewed joins in Spark'], ['Salting keys', 'Skew hints in Spark', 'Pre-filter the heavy key', 'Adaptive query execution'], ['Per-task duration p99', 'Per-key row counts', 'Executor idle time'], ['data']),
  F('fm-schema-drift', 'Schema drift', 'data-eng', 'de-quality', 'Upstream changes column types or names without notice; downstream silently breaks.', 'Common cause of dashboards mysteriously going wrong.', ['Producer renames a field', 'Type change e.g., int → string', 'Nullability becomes false suddenly'], ['Schema registry with compatibility rules', 'Contract tests on producers', 'dbt tests on landing tables'], ['Validation failures count', 'Null/type-error rate'], ['data']),
  F('fm-model-drift', 'Model drift', 'mlops', 'mlops-monitoring', 'Model performance degrades over time as the world changes.', 'A static model is a depreciating asset; you must monitor it.', ['Distribution shift in features (covariate drift)', 'Label shift (target marginal changes)', 'Concept drift (X→Y relation changes)'], ['Population stability index checks', 'Trigger retrains on drift', 'Shadow new models'], ['PSI', 'KL divergence', 'Live precision/recall vs holdout'], ['mlops']),
  F('fm-training-serving-skew', 'Training-serving skew', 'mlops', 'mlops-features', 'Features computed differently at train time vs serve time.', 'Causes silent quality regressions that look like drift but are bugs.', ['Different transformations in pipelines', 'Different timezone handling', 'Different data sources'], ['Feature store with one transformation', 'Reuse identical code path online + offline', 'Continuous parity checks'], ['Feature value distributions train vs serve', 'Prediction distribution drift'], ['mlops']),
  F('fm-data-leakage', 'Data leakage', 'ml', 'ml-eval', 'Information from outside the training set leaks into features, inflating offline metrics.', 'A model that looks great offline but fails online — almost always leakage.', ['Future-looking features', 'Target encoding without proper folds', 'Train/test split by random when group should be used'], ['Group-aware splits', 'Time-based splits', 'Audit feature creation timestamps'], ['Online vs offline metric delta'], ['ml']),
  F('fm-silent-corruption', 'Silent data corruption', 'data-eng', 'de-quality', 'Wrong data flows through with no errors thrown.', 'Worst kind — you find out from a customer or worse, an exec.', ['Bad upstream join exploding rows', 'Misaligned timezones', 'Duplicate ingest after retry'], ['Reconciliation checks vs source-of-truth', 'Anomaly detection on row counts', 'Strong primary keys + dedupe contracts'], ['Row count anomaly score', 'Reconciliation gap'], ['data']),
  F('fm-cascading', 'Cascading failures', 'distributed-systems', 'ds-failure', 'One slow service causes upstream to timeout, retry, and amplify load — collapsing the system.', 'Most large outages are cascades, not single-component failures.', ['Retry storm from upstream', 'Thread pool exhaustion', 'Bulkhead missing — one customer kills the rest'], ['Circuit breakers', 'Bulkheads', 'Capped retries with jitter', 'Hedging vs racing carefully'], ['Saturation per pool', 'Retry rate', 'Upstream timeouts'], ['ds']),
  F('fm-thundering-herd', 'Thundering herd', 'distributed-systems', 'ds-failure', 'Many clients wake up simultaneously and slam a downstream all at once.', 'Cron-aligned consumers, cache TTL coincidences, restart-after-outage all cause this.', ['Aligned cron schedules', 'Mass reconnect after outage', 'Same TTL on many cache keys'], ['Jitter on schedules and retries', 'Randomized TTLs', 'Token bucket admission control'], ['Concurrent request count', 'Connection establishment rate'], ['ds']),
  F('fm-retry-storm', 'Retry storm', 'distributed-systems', 'ds-failure', 'Failing requests are retried aggressively, multiplying load on the failing component.', 'Turns brownouts into full outages — retries are user-facing weapons.', ['Naive infinite retry', 'No exponential backoff', 'No jitter'], ['Exponential backoff with jitter', 'Bounded retries', 'Circuit breakers'], ['Retry attempts/sec', 'Upstream error rate'], ['ds']),
  F('fm-split-brain', 'Split brain', 'distributed-systems', 'ds-consistency', 'Network partition leaves multiple nodes thinking they are leader; divergent writes ensue.', 'Data corruption that is expensive to reconcile.', ['No quorum enforcement', 'Bad failover automation', 'Auto-failover during transient partitions'], ['Quorum / consensus (Raft, Paxos)', 'Fencing tokens', 'STONITH for clustered storage'], ['Leader election count', 'Divergent commit timestamps'], ['ds']),
  F('fm-network-partition', 'Network partition', 'distributed-systems', 'ds-consistency', 'Some nodes cannot reach others while both halves remain alive.', 'CAP theorem made physical — you must choose what to sacrifice.', ['Asymmetric reachability', 'Slow rather than failed links (gray failure)', 'Cross-AZ latency spikes'], ['Quorum-based protocols', 'Multi-AZ awareness', 'Graceful degradation paths'], ['Inter-AZ RTT', 'Partition detection counter'], ['ds']),
  F('fm-cold-start', 'Cold start', 'cloud', 'cloud-serverless', 'Serverless / autoscaled instance takes seconds to become hot — first request is slow.', 'Tail-latency killer for low-traffic services and bursty workloads.', ['Lambda init time', 'JVM warm-up', 'Model load time on GPU'], ['Provisioned concurrency', 'Smaller artifacts', 'Snapshot/SnapStart', 'Warm-up pings'], ['First-request latency', 'Init duration'], ['cloud']),
  F('fm-gpu-oom', 'GPU memory bottleneck', 'mlops', 'mlops-serving', 'Model + KV cache + batch exceed GPU VRAM and either crash or evict aggressively.', 'Defines serving cost and concurrency for LLM workloads.', ['Long context with growing KV cache', 'Batch too large', 'Memory fragmentation'], ['Paged attention (vLLM)', 'Quantization', 'Sequence length limits', 'CPU offload'], ['VRAM used', 'OOM events', 'Eviction count'], ['gpu', 'llm']),
  F('fm-hallucination', 'LLM hallucination', 'llmops', 'llm-eval', 'Model produces plausible but incorrect content.', 'Trust killer; in regulated domains, a legal risk.', ['Insufficient retrieval', 'Ambiguous prompts', 'Long outputs drift off rails'], ['Grounding via RAG', 'Citations required', 'Constrained outputs', 'Refusal training / guardrails'], ['Faithfulness', 'User feedback thumbs', 'Eval suite pass rate'], ['llm']),
  F('fm-prompt-injection', 'Prompt injection', 'genai', 'genai-security', 'Attacker text in retrieved content or user input overrides the system prompt.', 'New OWASP-class vulnerability with no fully solved defense.', ['Direct injection in user message', 'Indirect injection via retrieved docs or tool output', 'Jailbreak chains'], ['Untrusted/Trusted boundaries', 'Privilege separation for tools', 'Output filtering and policy LLMs', 'Don\'t mix instructions with data in prompts'], ['Detected injection attempts', 'Tool misuse rate'], ['llm', 'security']),
  F('fm-retrieval-failure', 'Vector retrieval failure', 'vector', 'vector-eval', 'RAG returns irrelevant or empty results.', 'Garbage retrieval ⇒ garbage answers; often invisible without dedicated evals.', ['Bad chunking', 'Mismatched embedding model', 'Stale index', 'Filter too restrictive'], ['Hybrid search', 'Reranking', 'Periodic eval suite with golden Q/A pairs'], ['Recall@k', 'Precision@k', 'Empty-result rate'], ['rag']),
  F('fm-embedding-drift', 'Embedding drift', 'vector', 'vector-eval', 'Embedding model updates change vector geometry without reindexing.', 'Quietly destroys retrieval quality after a model upgrade.', ['Mixing old and new embeddings in same index', 'Forgetting to reindex after model bump'], ['Pin embedding model versions per index', 'Backfill reindex on model change'], ['Retrieval precision regression on golden set'], ['rag', 'embeddings']),
  F('fm-observability-blind', 'Observability blind spots', 'observability', 'observ-blind', 'Critical user journeys lack instrumentation; you discover problems from customers.', 'You can only debug what you measured.', ['Missing trace propagation across services', 'No structured logs from a critical path', 'Async/queue boundaries lose context'], ['OpenTelemetry everywhere', 'Trace propagation contracts', 'Synthetic journey monitors'], ['Trace completeness ratio', 'Spans-per-request distribution'], ['observability']),
  F('fm-thread-pool', 'Thread pool exhaustion', 'backend-eng', 'be-concurrency', 'All threads blocked on slow I/O; new requests queue, then time out.', 'Classic blocking-IO failure that turns slow into down.', ['Synchronous calls to slow downstream', 'No timeouts on outbound calls', 'Unbounded queue in front of pool'], ['Timeouts on every outbound call', 'Async I/O', 'Bulkhead per dependency'], ['Active threads', 'Pool queue depth', 'Outbound call duration p99'], ['backend']),
  F('fm-clock-skew', 'Clock skew', 'distributed-systems', 'ds-time', 'Different nodes have different views of time; ordering and TTLs break.', 'Causes mysterious cache invalidation, auth, and consensus bugs.', ['NTP misconfiguration', 'VM pauses', 'Leap seconds'], ['Use logical clocks / HLCs', 'Strict NTP/PTP monitoring', 'Avoid wall-clock comparisons for ordering'], ['Per-host clock offset', 'NTP sync status'], ['ds', 'time']),
];

export const failureModeNodes = failureModes;
